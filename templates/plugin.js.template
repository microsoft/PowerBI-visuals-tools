var powerbi;
(function (powerbi) {
    var visuals;
    (function (visuals) {
        var plugins;
        (function (plugins) {
            plugins.debugVisual = {
                name: 'debugVisual',
                class: 'debugVisual',
                capabilities: powerbi.extensibility.visual.<%= className %>.__capabilities__,
                apiVersion: "<%= apiVersion %>",
                create: function (options) { return new powerbi.extensibility.visual.<%= className %>(options); }
        };
        })(plugins = visuals.plugins || (visuals.plugins = {}));
    })(visuals = powerbi.visuals || (powerbi.visuals = {}));
})(powerbi || (powerbi = {}));